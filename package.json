{
  "name": "jpa-site",
  "version": "1.0.0",
  "description": "JPA — JagoPakaiAI Hub SKILL.MD, DESIGN.MD & MCP untuk AI Agent Indonesia",
  "private": true,
  "scripts": {
    "dev": "eleventy --serve --quiet",
    "build": "eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
